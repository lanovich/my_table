# React Table Widget

Компонент таблицы с иерархическими данными, сортировкой и фильтрацией, построенный на архитектуре Feature-Sliced Design (FSD).

## 📋 Содержание

- [Архитектурные решения](#архитектурные-решения)
- [Функциональность](#функциональность)
- [Технологический стек](#технологический-стек)
- [Установка и запуск](#установка-и-запуск)
- [Демонстрация](#демонстрация)
- [Структура проекта](#структура-проекта)

## 🏗️ Архитектурные решения

### Feature-Sliced Design (FSD)

Приняли FSD архитектуру для чёткого разделения ответственности и масштабируемости:

- **`app/`** - Точка входа, глобальные стили, корневой компонент
- **`shared/`** - Переиспользуемые UI компоненты без бизнес-логики  
- **`widgets/`** - Сложные UI блоки (таблица) с бизнес-логикой
- **`entities/`** - Модели бизнес-сущностей (моковые платежи) и их UI представление

## ✨ Функциональность

### 🎯 Основные возможности

- **Генеричная таблица** - Работает с любыми данными через типы TypeScript
- **Иерархические данные** - Поддержка parent-child отношений с визуализацией дерева  
- **Сортировка** - Сортируемые колонки с тремя состояниями (asc → desc → none)
- **Фильтрация** - Boolean фильтры с визуальными индикаторами активности
- **Раскрывающиеся строки** - Expand/collapse

### 🔧 Фильтрация

- **Три состояния:** Все → Не активные → Активные
- **Визуальные индикаторы:** Белая иконка + красный бейдж при активации
- **Умное позиционирование:** Pop-up следует за кнопкой при прокрутке
- **Корректная иерархия:** Показывает только строки, прошедшие фильтрацию

### 🎨 Визуальные возможности

- **Адаптивное выравнивание** - Поддержка left/center/right для всех колонок
- **Визуализация дерева** - Линии связи между родительскими и дочерними элементами
- **Hover эффекты** - Визуальная обратная связь при взаимодействии
- **Статусные бейджи** - Цветовые индикаторы для boolean полей

## 🛠️ Технологический стек

- **React 19** - Последняя версия React с улучшениями производительности
- **TypeScript** - Полная типизация и безопасность типов
- **Vite** - Быстрая сборка и hot reload
- **CSS Modules** - Изолированные стили без конфликтов
- **Lucide React** - Современная библиотека иконок

## 🚀 Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/lanovich/my_table.git

# Установка зависимостей
npm install

# Запуск development сервера
npm run dev
```

### Доступные команды

```bash
npm run dev      # Запуск development сервера
npm run build    # Production сборка  
npm run lint     # Проверка кода ESLint
npm run preview  # Предпросмотр production сборки
```

## 📺 Демонстрация

### Видео демонстрация

[![Demo GIF](/assets/demo.gif)

*Основные сценарии демонстрации:*

1. **Фильтрация по активности** - Показаны все → только неактивные → только активные
2. **Сортировка по колонкам** - Сортировка по email, балансу, имени
3. **Иерархическая работа** - Раскрытие узлов и фильтрация дочерних элементов  
4. **Визуальные индикаторы** - Белые иконки и бейджи при активных фильтрах

### Пример использования

```typescript
const columns: Column<Payment>[] = [
  { key: "name", label: "Имя" },
  { 
    key: "email", 
    label: "Email",
    sort: (row) => row.email.toLowerCase()
  },
  {
    key: "balance",
    label: "Баланс", 
    align: "right",
    sort: (row) => parseFloat(row.balance.replace(/[$,]/g, "")),
  },
  {
    key: "isActive",
    label: "Активность",
    align: "center",
    renderCell: (row) => (
      <StatusBadge isActive={row.isActive} />
    ),
    filter: {
      type: 'boolean',
      falseLabel: "Не активные",
      trueLabel: "Активные"
    }
  }
];

<Table
  columns={columns}
  rows={rows}
  getChildren={(row) => rows.filter(r => r.parentId === row.id)}
/>
```

## 📁 Структура проекта

```
src/
├── app/                          # Слой приложения
│   ├── App.tsx                  # Корневой компонент
│   ├── main.tsx                  # Точка входа React
│   └── App.module.css            # Глобальные стили
│
├── shared/                       # Переиспользуемые компоненты
│   ├── lib/                      # Бизнес-логика
│   │   ├── filtering.ts          # Утилиты фильтрации
│   │   ├── sorting.ts            # Утилиты сортировки
│   │   └── index.ts
│   ├── hooks/                    # Кастомные хуки
│   │   ├── useClickOutside.ts    # Обработка кликов вне элемента
│   │   └── index.ts
│   └── ui/                      # UI компоненты
│       ├── StatusBadge/           # Индикатор статуса
│       ├── Cell/                  # Ячейка таблицы
│       ├── FilterDropdown/        # Dropdown для фильтров
│       ├── BooleanSelect/         # Select для boolean значений
│       └── index.ts
│
├── widgets/                     # Сложные UI виджеты
│   └── Table/                   # Виджет таблицы
│       ├── model/                 # Типы и интерфейсы
│       └── ui/                    # Компоненты таблицы
│
└── entities/                    # Бизнес-сущности
    └── payment/                 # Сущность платежа
        ├── model/                 # Типы и моковые данные
        └── ui/                    # UI представление
```

## 🎯 Ключевые преимущества

- **Масштабируемость** - FSD архитектура позволяет легко добавлять новые сущности
- **Переиспользование** - Shared компоненты можно использовать в других проектах  
- **Типизация** - Полная TypeScript безопасность на всех уровнях

## 📄 Лицензия

MIT License - свободно для использования в коммерческих проектах.